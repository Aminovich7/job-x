# Plan: Create CODE_EXPLANATION.md

## Goal
Create a comprehensive senior-developer-level code explanation document (`CODE_EXPLANATION.md`) for the job-x Freelance Marketplace API project.

## File to Create
- `CODE_EXPLANATION.md` (root of project)

## Content Structure

The document will cover 9 sections:

1. **Project Overview** — What the project does, tech stack
2. **Architecture Decisions** — Why FBVs (not ViewSets), why no signals for contract creation, why `conf` package name
3. **Models** — Every field's purpose and why the specific field type was chosen (e.g., `DecimalField` over `FloatField` for money, `OneToOneField` for 1:1 relationships, `DateField` vs `DateTimeField`)
4. **Serializers** — 7 patterns used: `write_only` passwords, custom `create()` for hashing, read-only response serializers, context injection, empty-data validation, field-level validation, plain serializers for schema docs
5. **Views** — FBV architecture, decorator stack order, `enforce_permission()` helper bridging FBVs with object-level permissions, `transaction.atomic()` usage
6. **Permissions** — The two-tier system (decorator-level coarse checks + mid-function fine-grained object checks), all 7 permission classes explained
7. **URL Routing** — Root and app URL structure, design choices
8. **Settings** — JWT-only auth, global `IsAuthenticated` default, PostgreSQL config, auto field choice, drf-spectacular
9. **Coding Style Conventions** — View structure pattern, naming conventions, imports, no-comments approach, known issues

## Verification
- After creating the file, confirm it exists with `ls`
- Read it back to verify content completeness
